#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @n:input character
 * Return: Always 0.
 */

int _abs(int)
{
	int num = n;
	
	if (num > 0)
	{
		return (num);
	}
	else if (num < 0)
	{
		return (-num);
	}
	
	return (0);
}
