#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @n:input character
 * Return: Always 0.
 */

int _abs(int)
{
	int n;
	if (n < 0)
	{
		n = (-1) * n;
		return (n);
	}
	else
	{
		return (n);
	}
}
